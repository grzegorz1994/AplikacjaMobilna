<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.organizerrolnika">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".PowiadomieniaActivity"></activity>
        <activity android:name=".NotatkiDoPola" />
        <activity android:name=".InformacjeOPolu" />
        <activity android:name=".OchronaRoslinActivity" />
        <activity android:name=".NawozenieMineralneActivity" />
        <activity android:name=".NawozenieOrganiczneActivity" />
        <activity android:name=".RodzajNawozeniaActivity" />
        <activity android:name=".WybierzZabiegActivity" />
        <activity android:name=".MenuPolaActivity" />
        <activity android:name=".CreateFieldActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>